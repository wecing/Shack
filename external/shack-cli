#!/usr/bin/env python
#! -*- coding: utf-8 -*-

import sys, os

def add_song(song_id):
    s = "osascript -e 'tell app \"Terminal\" to display alert \"%s\"'"
    os.system(s % song_id)

if __name__ == '__main__':
    for song_id in sys.argv[1:]:
        add_song(song_id)
